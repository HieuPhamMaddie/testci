version: 2.1
orbs:
  k8s: digitalocean/k8s@0.1.1

jobs:
  install:
    steps:
      - k8s/install
workflows:
  install_tools:
    jobs:
      - install:
          filters:
            branches:
              only:
                - main # Thay đổi thành nhánh bạn muốn triển khai
